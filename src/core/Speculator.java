package core;

public class Speculator extends Actor{
    public Speculator(int actor, String actorType, String name, String main_role, String organisationType) {
        super(actor, actorType, name, main_role, organisationType);
    }
}
